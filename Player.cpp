#include "Player.h"

Player::Player(float hp, float sc, float ms, float vv, float k, float s, Camera3D c) : health(hp), shotCooldown(sc), moveSpeed(ms), verticalVelocity(vv), kills(k), shots(s), camera(c) {}
